# Dependencies for the ML challengers (ml/) and the frozen panels. The
# stdlib-only scoring core (scoring/) does NOT need these.
#   pip install -r ml/requirements.txt
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
# The interactive model builder (Dash) has its own deps:
#   pip install -r tools/model-builder/requirements.txt
