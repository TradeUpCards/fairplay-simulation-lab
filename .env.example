# Copy to .env and fill in. Never commit .env.
# Stack is not yet chosen — keys below are placeholders for the AI Investigator layer.

# ANTHROPIC_API_KEY=

# Gemini ("nano banana") image generation — used by the ce-gemini-imagegen skill
# to generate the lobby avatar cast (frontend/src/assets/avatars/). Get a key at
# https://aistudio.google.com/apikey. Build-time only; never shipped to the browser.
# GEMINI_API_KEY=
